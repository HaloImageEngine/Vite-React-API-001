This React project uses AXIOS to get the rest api https://dummyjson.com/products -- Read detail docs from https://dummyjson.com/docs
npm install 
npm install axios
npm install react-router-dom
0. Install Material UI
npm install @mui/material @emotion/react @emotion/styled

1. Install Tailwind CSS via npm

npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
This will create:
tailwind.config.js
postcss.config.js

If you have problems with tailwind. 
Then 
🔝 1. Chakra UI
Type: Component-based UI library
Features:
Fully styled accessible React components
Dark mode support
Styled System-based design tokens
Why Choose It: Clean design + easy to customize + good DX
npm install @chakra-ui/react @emotion/react @emotion/styled framer-motion